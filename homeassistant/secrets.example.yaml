# Home Assistant secrets scaffold for GitHub release checks.
# Copy these keys into your real HA /config/secrets.yaml.
#
# Only needed when repository is private. For public repos, 06_ota.yaml no longer requires auth.
github_releases_authorization: "Bearer ghp_your_token_here"
